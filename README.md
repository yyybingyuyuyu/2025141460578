# NCurses Tetris C++
Original binary block compression logic ported from C to C++.
Support GitHub Codespaces / Linux / macOS.

## Build & Run
1. Install dependency
```bash
sudo apt install libncurses5-dev g++
